{
  "scripts": {
    "start": "json-server --watch db.json --static ./videos --port 3001",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
